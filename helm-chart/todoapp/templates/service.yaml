apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-svc" (include "todoapp.fullname" .) }}
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app.kubernetes.io/name: {{ include "todoapp.name" . }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
