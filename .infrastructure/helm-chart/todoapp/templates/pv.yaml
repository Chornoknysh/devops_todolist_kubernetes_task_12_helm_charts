apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ include "todoapp.fullname" . }}-pv
spec:
  capacity:
    storage: {{ .Values.pv.capacity }}
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data/{{ include "todoapp.fullname" . }}"
